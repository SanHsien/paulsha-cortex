### Fixed
- **Issue #465：workflow-lane handoff manifest 未寫 repo 歸屬，`recent_done` 永遠 `repo=null`**：#230（PR #349）補了讀取端——`manager_daemon._repo_from_manifest` 依 `repo`／`workflow_repo`／`work_authority.repo` 三鍵投影、缺鍵明確回 `null` 不從 branch 推斷——但 workflow-lane 的寫入端沒跟上：`manager.complete_tick` 的終局 manifest dict（`handoff.write_manifest` 唯一呼叫端，build 與 review kind 共用）沒有任何 repo 歸屬欄位，即使 job record 派工當下就帶著 `workflow_repo`（`dispatch_workflow_card` 的 `registry.create_job(..., workflow_repo=run.repo)`，兩種 kind 皆有）；`work_authority` 只有 slice-lane delivery 的 completion record 才會出現，workflow-lane 碰不到，故 job record 的 `workflow_repo` 是唯一可行歸屬來源。修法為 manifest dict 補一欄 `"workflow_repo": job.get("workflow_repo")`：workflow-lane 終局後 manifest 帶 repo 歸屬、`recent_done` 條目 `repo` 投影為該 workflow 的 repo（如 `hamanpaul/paulsha-cortex`）；slice-lane job 無此欄寫入 `null`，讀取端行為不變；舊 manifest 缺鍵維持 `repo=null`（#230 不推斷契約）。下游 paulshaclaw cockpit（hamanpaul/paulshaclaw#264）已前向相容 `repo` 欄，不需改動即自動顯示。新增回歸測試：workflow-lane build／review kind 終局 manifest 含 `workflow_repo`、slice-lane manifest 該欄為 `null`、`recent_done` provider 自 `workflow_repo` 鍵投影 `repo`、缺鍵 manifest 仍 `repo=null`。
