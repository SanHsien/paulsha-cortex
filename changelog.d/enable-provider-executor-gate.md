ship-phase 兩張卡（`openspec-archive`／`policy-commit`）的 `runtime_capabilities` 加宣告 `provider:executor` 動態 sentinel（#369 建立、原本無卡消費），小範圍試啟用 dispatch 前的 executor 登入態閘門：preflight 逐 identity candidate 解析成其實際 executor 並經 `coordinator.executor_auth` 探測，限流／登出擋在 model session spawn 之前並依既有順序 re-route。同步更新 `manager.py` hold 註解（現況已翻轉，其餘卡待 ship-phase 觀測無誤再擴大），並補 manager 接線層 regression（注入 fake runner，不實體 spawn）（issue #442）。
