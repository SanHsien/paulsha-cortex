新增 `docs/superpowers/specs/default-envelope-values-spec.md`（#453，#452 子項）：定案無 patchmud 實測時封套四欄位的保守預設——`accepts_bands` 依 persona 定為 builder/reviewer `[green, yellow]`、planner 全值域含 red（推導自 #223 red 攔截鏈與 needs_decomposition 回派路徑）；`invariant_ceiling` 走 bypass 例外（值域無界且歷史分布經查證不存在，任何有限數都違反逐位元驗收）；`consistency_scope`／`acceptance_modes` 取 #209 R2 全值域（封閉有限值域存在上確界）；存放機制定案 `DEFAULT_ENVELOPE` per-persona 常數於查表投影套用、registry 檔案永不寫入預設值；並給出 #452 實作必須照做的 bit-identical 回歸測試規格（golden 雙配置決策軌跡／預設值恆不排除 property test／loader 相容）。
