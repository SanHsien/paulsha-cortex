### Fixed
- **Issue #378：builder 產出的「實證」可能是 rigged setup，verification gate 全綠也擋不住**：驗收契約字彙只有 `persona-scope` 與 `command`（`paulsha_cortex/coordinator/verification.py`），`run_result_verification` 只驗 git ancestry／worktree 乾淨度／artifacts 存在性／scope diff／exit code，`required_artifacts` 只驗 `exists()`＋path in changed、不看內容——gate 提供的保證是「跑得起來且測試綠」，不含「結論站得住腳」。2026-08-07 對 `hamanpaul/embedebuguide` 派工 issue #47 時實際發生：兩支 evidence-case probe 的「provider vs naive」對照臂共用可變狀態（`case2_transient_burst.py` 兩臂共讀同一個 PTY，provider 先讀空）或判準本身是恆真式（`case4_exclusive_lease.py` 用兩個 `sleep()` 的 wall-clock 重疊當「爭用同一資源」的證據，實際沒有任何共享資源），差異由 setup 順序而非被測性質產生，卻仍拿到全綠 gate。複驗確認 `adversarial-review` 卡與 `review.py` 的 `BLOCKING_FINDING_CATEGORIES`（已含 `acceptance`／`verification-bypass`）fail-closed 通道皆已存在，缺的只是（a）卡片任務描述沒有指示對抗式檢視 rigging，（b）本 repo 現有的 evidence-claim 類 combo（`mcu-feature`，硬體證據）沒有掛上這張卡。不新建 persona、不新建機制、不動 `run_result_verification`：
  1. `adversarial-review` 卡（`paulsha_cortex/deck/data/cards.yaml`）新增 `execution.action`，明確指示對抗式檢視 evidence／實證是否為 rigged setup——(a) 對照臂是否共用 mutable state／一次性資源使差異由 setup-order dependency 產生；(b) verdict 判準是否為恆真式；(c) 結論是否真由觀測值導出而非預設寫死——任一項成立即以 `verification-bypass` 或 `acceptance` category 回報 blocking finding。
  2. `mcu-feature` combo（`paulsha_cortex/deck/data/combos/mcu-feature.yaml`，本 repo 現有的 evidence-claim 類 combo）把 `adversarial-review` 直接掛進核心層（`combo.cards` / `combo.gate_spine`，接在 `code-review` 之後、`receiving-code-review` 之前），不透過 `band_triggered` 加掛層——此 combo 本無 band 概念，evidence-claim 類 slice 因此不論 band 評估結果都會派工，不會像 `feature-oneshot` 的 band-triggered 加掛層那樣被 Green band 跳過。
  3. 複驗確認 `validate_review_verdict` / `validate_gate_evaluation`（`paulsha_cortex/coordinator/review.py`）本就不吃 `card_id`，`verification-bypass`／`acceptance` blocking finding 早已使 `state` 落 `rejected`、`manager.py` 的 workflow review 分支（`:7681-7721`）早已把 `rejected` 轉成 `gate_result=needs_human`；新增回歸測試直接驗證這條既有通道對 rigged-evidence 場景成立，未額外新建接線。

  新增／調整測試：`tests/test_deck_data.py`（`adversarial-review` 任務描述含 rigging 檢視語意；`mcu-feature` combo 結構與 compile 產出的 slice／verify_commands／gate_spine）、`tests/test_coordinator_foreign_review.py`（`verification-bypass`／`acceptance` 兩個 category 皆使 `validate_review_verdict` 落 `rejected`；`build_gate_evaluation`→`validate_gate_evaluation` 端到端串接驗證同樣 fail-closed）。修正前已確認重現 RED（`adversarial-review` 缺任務描述、`mcu-feature` 未強制掛卡）。全套 pytest：2179 passed、32 subtests passed（基線 2175 passed、32 subtests passed，淨增 4）。
