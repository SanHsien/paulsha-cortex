### Fixed
- **Issue #418：#414 materialize 出的 canonical plan 檔與 brainstorm evidence 對帳必炸**：`_validated_brainstorm_planning_authority` 以 brainstorm evidence 的 `artifacts` 重算 `scanned` 後，單純用 `set(persisted) - set(scanned)` 非空即 `raise ValueError("workflow brainstorm evidence omits persisted authority")`；但 `_materialize_plan_card_output`（#414）為了對齊 build 端 declared input pattern（`docs/superpowers/plans/*<slug>*.md`），會把已 accepted 的 kind=plan 內容原樣 materialize 到 canonical 路徑並併入 `run.planning_authority`——這份副本天生不在 brainstorm evidence 列出的 artifacts（實際發佈路徑通常是 `docs/superpowers/workstreams/<slug>/todo.md`）裡，落入差集後每次 resume 對帳必 `needs_human`。修法：在最終 omission 判定前，對 `persisted - scanned` 差集中的每個 ref 逐一檢查是否為「合法 materialized 副本」——`kind == "plan"`、`work_id` 與 run 相符、`baseline_sha256` 與某個已驗證的 scanned kind=plan entry 完全相同（byte-copy）、且 ref 落在 plan phase 宣告的 output pattern（`fnmatch`）內；四條全符合才排除於 omission 之外，其餘維持 raise。回傳形狀不變：`ordered` 仍以 `run.planning_authority`（即 `persisted` 的來源）為起點，被排除的合法副本原樣留在回傳的 authority tuple 內，繼續透過 `_workflow_input_snapshot` 的 authority_refs fallback seed 進 build worktree。新增 3 個回歸測試：byte-copy＋pattern 相符的合法副本不 raise 且回傳值保留該 entry（修正前確認 RED，重現 `omits persisted authority`）；digest 不同（非 byte-copy，真 omission）仍 raise；ref 不落在 plan output pattern 內仍 raise（防止例外路徑被濫用成任意 ref 的漏洞）。
