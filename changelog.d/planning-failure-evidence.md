### Fixed
- **Issue #393：recover-planning 的 evidence 生產端不存在，`cortex-planning-failure/v1` 全庫僅有 reader**：`work_actions._read_planning_failure_record`／`_planning_failure_hint` 要求 `run.evidence_refs` 內存在 `planning-recovery/` 目錄下、schema `cortex-planning-failure/v1`、run_id 相符的 evidence 檔，但 `manager.apply_workflow_action` 的 define 三條 needs_human 靜默失敗路徑（`runtime_factory` 初始化例外、runtime 元件缺失、`run_heterogeneous_brainstorm` 未收斂到 ready——即 #391 已補 `logger.error` 的同三個點位）過去沒有任何一條寫 evidence 或 append `evidence_refs`，導致 `recover-planning`（issue 256 設計的 planning 恢復動作）對它最該覆蓋的「define 靜默失敗」場景結構性不可用，resume 的 next_actions 只剩 `abandon`，把可恢復故障升級成不可逆棄單。修法：新增 `manager._write_planning_failure_evidence`（原子寫入模式與 `work_bridge._write_json_evidence` 一致——tmp、fsync、rename、0400；body 直接落 reader 要求的頂層欄位，不套 envelope）與呼叫端 wrapper `_record_planning_failure_evidence`（evidence 寫入失敗 fail-open，只 log 不炸 define 路徑），三條路徑各自落一筆 evidence（`run_id`／`classification`／`reason`／`created_at`）並與既有 facets 更新合併成同一次 `_manager_update_workflow_run`（避免多次獨立 persist）：runtime 初始化例外與元件缺失歸 `environment`，brainstorm 未 ready 歸 `content`（`_resume_decision` 對 `content` 仍 fail-closed 不浮現 `recover-planning`，符合 issue 256 原始設計意圖）。**未做**歷史遺留、無 evidence 的 needs_human define run 的例外放行通道（issue 393 評估項 2）——維持 fail-closed，由既有 abandon＋authority digest 前進路徑處理遺留 run。新增回歸測試釘住三條路徑：evidence 檔存在於 `planning-recovery/`、schema/run_id/classification/reason 正確、`run.evidence_refs` 含該路徑、`_read_planning_failure_record` 可讀、`_planning_failure_hint` 回非 None；修正前皆確認 FAIL。
